# Experience OS 版本规划

> 来源:001_PRD/version、001_PRD/lark(ChatGPT 建议)+ 已落地原型
> 写法:**能力演进 ×平行技术线**(产品路线图,非功能堆砌)

## 产品定位

**Experience OS —— 个人经验管理与决策辅助引擎(Personal Experience Engine)。**

- **采集层(Capture)**:飞书 / 微信 / 浏览器插件 / App …—— 负责"记录",**是入口,不是产品**。
- **智能层(Intelligence)= Experience OS**:负责**提炼 → 关联 → 验证 → 决策辅助**。这层才是护城河。

> 一句话:**让每一次经历,都变成下一次决策的资产。**
> 我们解决的不是"今天发生了什么",而是"如何避免明天重复犯同样的错"。

## 两条演进线

| 产品能力演进 | 技术能力演进 |
|------|------|
| V1 经验提炼 | 本地存储 |
| V2 经验关联(规律发现) | 用户自定义 API Key / 多模型 |
| V3 经验验证 | 多模型支持 |
| V4 决策辅助 | 本地向量检索 |
| V5 组织经验 | 飞书生态接入 |

---

## 版本详情

### V1 经验提炼(比赛版本)
- **目标**:把碎片化、口语化经历转成结构化经验。
- **能力**:自由输入 / AI 经验提炼 / 自动标签分类 / 经验库沉淀 / 周·月复盘。
- **原则**:Local First(本地优先)· AI Assisted(AI 辅助提炼,不替代思考)· User Ownership(经验归用户)。

### V2 经验关联(规律发现)
- **目标**:发现经验之间的联系。
- **能力**:相似经验聚类 / 高频问题识别 / 成功模式识别 / 时间维度分析。
- **价值**:让用户看见自己难以察觉的重复模式("过去 90 天:目标不清晰 12 次")。

### V3 经验验证
- **目标**:让经验从"观点"变成"经过验证的策略"。
- **能力**:验证次数统计 / 成功率分析 / 可信度评分 / 策略演化记录。
- **价值**:建立属于自己的高可信决策库。

### V4 决策辅助
- **目标**:在未来关键场景主动调用历史经验。
- **能力**:场景识别 / 风险提醒 / 历史经验召回 / 决策建议。
- **价值**:减少重复犯错("这门高价课你过去买过 6 次,完成率仅 25%")。

### V5 组织经验(生态)
- **目标**:从个人经验网络扩展到团队/组织经验网络。
- **能力**:飞书机器人接入 / 多维表格同步 / 团队经验库 / 组织知识资产。
- **价值**:个人经验向组织经验传递与复用。

---

## 当前实现范围(本阶段只做"部分")

> 更新(2026-06-24):**V1–V4 产品能力已全部落地/产品化**(见下表);并行的「本地语义引擎」技术线方案就绪、其中 **A6 隐私脱敏已实现**;**V5(组织/飞书)已出可行性调研、维持推迟**。

| 能力 | 当前状态 | 说明 |
|------|:--:|------|
| V1 经验提炼 | ✅ 已实现 | 真模型(DeepSeek)提炼 + 三态分类(策略/避坑/待观察)+ 经验库 |
| V1 周·月复盘 | ✅ 已实现 | `periodicReview` 本周/本月摘要(记录数 + 高频问题/成功 + 一句话建议),规律发现面板内 |
| V2 规律发现 | ✅ 已实现(语义版) | `lawDiscovery` 两段式语义聚类(模型归因 + 统计降级)→ 持久化 Law 库 + 复发/趋势 + 生命周期(复盘/解决/复发回激活);`patternDiscovery` 统计卡作降级层 |
| V3 经验验证 | ✅ 已产品化 | 引擎(`evaluationEngine`)+ 就地极简复测:规则卡「上次管用吗?有效/无效/不确定 + 可选一句」→ `trustSignal` 即时演化;规则库「待验证」优先;黑话仍折叠在「高级」 |
| V4 决策辅助 | ✅ 已产品化 | 找经验召回(关键词 + 模型语义)+ 风险前置 + `trustSignal` 可信度 + **决策建议合成**(本地确定性 倾向/谨慎/证据不足 + 战绩,opt-in AI 润色);`decisionHints` 录入时召回为底座 |
| V5 组织/飞书 | ⬜ 推迟(已调研) | 采集层未来项;飞书多维表格**单向入站导入**已出可行性报告(`docs/superpowers/specs/2026-06-24-feishu-bitable-import-feasibility.md`),可行但暂不实现 |
| 技术:本地存储 | ✅ | localStorage 为唯一真相源 |
| 技术:自定义 Key/多模型 | ✅ 基础版 | DeepSeek 内置 + 用户可配;多家厂商接口已抽象 |
| 技术:隐私脱敏(A6) | ✅ 已实现 | `src/services/privacyFilter` 浏览器端脱敏(PII+Luhn / 205 条密钥规则 / 上下文+香农熵),接在**全部模型调用发送前**;移植自 privacy-filter |
| 技术:本地语义引擎(A1–A9) | 🟡 方案就绪 | 离线算法升级(分词/向量/聚类/召回/趋势/情绪),强化"本地优先"降级;见下方技术线 + `docs/algorithm-*.md` |
| 技术:向量检索 / 飞书 | ⬜ 推迟 | A4(端上向量)/ V5(飞书)再做 |

## 技术线:本地语义引擎(离线算法升级)

> 正交于产品 V 线:把"无模型时"的本地降级链(现为弱关键词)换成经典算法,强化「本地优先」红线 + 隐私。总览 `docs/algorithm-upgrade-plan.md`,各 `docs/algorithm-a*.md` 为 implementation-ready 方案。

| 项 | 算法 | 状态 | 强化 |
|----|------|:--:|------|
| A6 | 隐私脱敏(Luhn + 香农熵 + 正则 + gitleaks) | ✅ 已实现 | 隐私红线(发模型前脱敏) |
| A1 | 中文分词(HMM + Viterbi) | ✅ 已实现 | `src/services/segmentation.ts`;下游集成待后续 |
| A8 | 情绪分类(朴素贝叶斯) | ✅ 已实现 | `src/services/sentiment.ts`(否定处理+平滑);inferDirection 集成待后续 |
| A7 | 趋势检验(Mann-Kendall) | ✅ 已实现 | `src/services/trend.ts`;Law.trend 集成待后续 |
| A2+A3 | TF-IDF + DBSCAN 聚类 | ✅ 已实现 | `tfidf.ts` + `dbscan.ts`;lawDiscovery 集成待数据量/后续 |
| A9 | TextRank 主题词 | ✅ 已实现 | `src/services/textrank.ts`;集成待后续 |
| A4 | 端上 embedding 召回 | ✅ 核心已实现 | `embeddingRecall.ts`(Embedder 接口可测核心);真实模型适配器留后续 |
| A5 | ANN(LSH) | ✅ 已实现 | `src/services/ann.ts`(随机超平面 LSH,确定性);独立工具未接入 |

> **优先顺序(性价比,不吃数据量优先)**:A6(已 done)→ A1 → A8 → A7;A2/A3/A4 待数据量起来再做(冷启动分档见 `docs/algorithm-evaluation-and-data-plan.md`)。

---

### V1 非目标(本阶段坚决不做)
团队知识库 · 多人协作 · AI 教练 · 第二大脑 · RAG 平台 · MCP 生态 · 飞书接入 —— 全部推迟到 V5+。

### V1 缺口已补齐
**周·月复盘摘要**(`src/services/periodicReview.ts`):按周/月窗口统计"记录 N 条 / 高频问题 / 高频成功 / 一句话建议",已接入规律发现面板。至此 **V1 能力闭环**(采集→提炼→经验库→复盘→规律发现)。
