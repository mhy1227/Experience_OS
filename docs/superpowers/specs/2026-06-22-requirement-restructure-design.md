# Experience OS 需求结构调整设计

> 日期:2026-06-22(已经需求确认 + 复审定稿)
> 状态:已确认,可进入实施计划
> 来源:多 agent 梳理(产品/数据/AI/演示)+ ChatGPT 建议(001_PRD)+ 既有代码分析 + 需求确认对齐
> 约束:≈5 天;现有代码基本完成,**不重写已完成部分,只补缺口**

## 0. 定调(需求确认结论)

- **产品优先,比赛只是展示窗口**;不为 demo 造假数据/假结论。
- **5 天目标 = 一个本地能跑通的真实产品切片**(真模型管线 + 真规律发现);云写入/部署上线**后置可选**。
- 内置默认模型 = **DeepSeek API**;用户自配模型 = 后续(模型无关)。
- 演示**联网**,真模型现场可跑;本地引擎仅作工程降级,不是演示前提。

---

## 1. 方向迁移

```
现在:  一句话录入 → 单条规则 → 【庞大复测/采用/评估体系】(≈70% 复杂度,过度工程化)
目标:  录入/批量导入 → 经验提炼(真AI) → 跨记录【规律发现】→【决策辅助提醒】
```
差异化:日记沉淀事件,Experience OS 沉淀规律;日记帮回忆,我们帮决策。

---

## 2. 模块骨架

| 模块 | 状态 | 说明 |
|------|------|------|
| **M1 经验捕获** | 复用+扩展 | 输入扩到工作/沟通/投资/情绪事件;**新增批量导入(粘贴历史→拆条)解决冷启动** |
| **M2 经验提炼** | 升级 | DeepSeek 真模型(复用契约层);本地引擎降级为兜底 |
| **M3 规律发现** | 🆕 核心 | **统计聚类(可靠基座)+ 模型对簇做归因/命名/建议**;最小样本门槛防过度断言;纯统计兜底 |
| **M4 决策辅助** | 🆕 核心 | 录入新行动时主动召回历史规律并提醒(相关度阈值、可忽略) |
| **M5 产品原则层** | 部分有 | 本地优先(数据只在本机、可见化)+ 模型无关(DeepSeek 内置 + 用户 Key)+ 导出"经验资产" |
| ~~评估体系~~ | **降级隐藏** | 不删代码,从主路径摘除,折叠为"高级面板";其评分逻辑可复用为 Insight 可信度后端 |

---

## 3. 增 / 删 / 改

### 新增(进 5 天)
- **[增] 批量导入/冷启动**:粘贴一段文字/聊天/日记 → 批量拆成多条观察;引导式首录入;强化 demo 数据载入。
- **[增] M3 规律发现**:跨记录归因卡片(高频根因 + 百分比 + 证据链 + 决策建议),真实计算非预置。
- **[增] M4 决策辅助**:命中历史模式时提醒。
- **[增] M5 API Key 配置**:DeepSeek 内置默认;设置页可填用户 Key、切换 Provider;Key 存本地。
- **[增] 信任产品化**:界面明示"数据只在本机"、一键导出经验资产(markdown)、一键清空。
- **[增] 留存(轻量)**:低摩擦快速录入(产品优先,故纳入轻量版)。

### 砍 / 降级(代码保留不删,仅从主路径摘除)
- **[降] 复测矩阵 / 采用门槛 / 可重复性 / 样本独立性 / 协议合规 / 维护回归 / 恢复观察期** → 折叠"高级面板"或隐藏。
- **[降] 复测三态** → 规则卡上轻量"这条还准吗"反馈。
- **[降] CSV 导出** → JSON/markdown 导出保留(经验资产)。
- **[删] 死文件** `src/manifest.json`、`src/pages.json`(uni-app 残留,本项目纯 Vite,不参与构建)。

### 改写
- **[改] 定位文案** → "让经历变成决策资产 / 让每次踩坑产生复利"。
- **[改] 输入接纳范围** → 工作/沟通/投资/情绪。
- **[改] 演示数据** → 种子数据里**埋一个真实共同根因**,让真实管线自然跑出 80% 归因(不写死结论)。
- **[改] 默认 AI 入口** → DeepSeek 内置开箱即用,失败回退本地引擎。

---

## 4. 数据层

现有 7 张 `exp_` 表覆盖:经验闭环、成功率、时间趋势、按类目频次。调整:
- **[改] `exp_observations` 加结构化情绪列** `sentiment`(可聚合、建索引)+ 可选 `sentiment_score`/`emotion`;**不塞 tags**。
- **[增] `exp_insights` 表(核心)**:跨记录归纳的洞察,语义独立于 rule,与 rules/observations 多对多。列:`type`、`title`、`summary`、`confidence`、`time_window`、`metrics JSON`、`status`、`generated_by`、`evidence_rule_ids JSON`、`evidence_observation_ids JSON`。
- **[增] `exp_model_configs` 表**:`provider`、`model`、`base_url`、`api_key_ref`(**绝不存明文**)、`is_active`、`is_default`。
- **[原则] 本地优先**:**5 天内 localStorage 为唯一真相源**(app 数据落本地);云 MySQL 暂仅作工具/演示后端,**不实现同步**(后置);**API Key 永不上云**。

---

## 5. AI 流水线

**直接复用(无需重写)**:`ObservationModelClient` 接口、`analyzeObservationWithModel()`、`OBSERVATION_ANALYSIS_PROMPT`(防注入+方向约束)、`normalizeModelAnalysis`/`enforceAnalysisContract`/`assertValidAnalysis`(方向一致性 + 降级链)。

**实现要点**:
1. `createModelClient(config)`:先实现 **DeepSeek(OpenAI 兼容格式)**;用户 Key 同格式复用。
2. store 接入点:模型优先,失败回退本地 `analyzeObservation`,错误对用户可见。
3. **M3 规律发现**:① 统计聚类(按 category/tag/sentiment/根因维度)→ 可靠基座;② 对每个达到**最小样本门槛**的簇,调模型做归因/命名/决策建议;③ 模型不可用时退回纯统计描述。**绝不在样本不足时硬出强结论**。
4. 三段式 Prompt:第一段(提炼)必做;第三段(规律发现/归因)进 MVP(产品核心价值);第二段(归类)用统计聚类替代,模型增强后置。
5. Key 存 localStorage,导入导出排除 Key。

---

## 6. 演示(基于真实产品,不造假)

种子数据里埋一个真实共同根因 → 用**真实管线**跑出归因。

| 时间 | 屏幕 | 话术 |
|------|------|------|
| 0:00–0:30 | PPT 痛点:"为什么你总在同一个坑栽?" | "我们以为在进步,其实在重复犯错,只是看不见模式。" |
| 0:30–1:10 | 经验列表:**用批量导入预置 30–40 条跨 90 天工作观察** | "这是我 90 天随手写的片段,从没整理。" |
| 1:10–1:50 | 现场输入 1 条 → DeepSeek 实时生成策略卡 | "今天又记一条,看起来跟上次没关系吧?" |
| 1:50–2:40 ★ | 点「扫描我的 90 天」→ 真实管线产出归因卡:"38 条里 80% 负面结果指向同一根因:目标不一致" + 证据时间线 | (停顿)"AI 说 80% 是同一个原因,我自己没意识到。" |
| 2:40–3:10 | 卡片给可复用规则 + 决策建议 | "它还给了下次的动作,经验变资产。" |
| 3:10–3:30 | 三大原则页 + "数据只在本机" | "AI 是分析者,不是拥有者。" |

**演示中不碰**:复测矩阵/采用门槛/维护回归/协议合规、地图页、CSV 导出。

---

## 7. 5 天排期(产品切片)

| 天 | 工作 |
|----|------|
| D1 | DeepSeek client + store 接入 + 失败回退;M1 输入扩范围 + 文案 |
| D2 | 批量导入/冷启动(粘贴→拆条);情绪列 + 数据基座 |
| D3 | M3 规律发现:统计聚类 + 模型归因 + 最小样本门槛 + 「扫描 90 天」归因卡 |
| D4 | M4 决策提醒;Key 配置 UI;评估体系折叠隐藏;删死文件 |
| D5 | M5 信任产品化(本地优先可见 + 导出经验资产);种子数据(埋根因);彩排 |

**后置**:云写入/同步(P0 服务端代理)、模型做经验归类(第二段)、embedding 语义聚类、评估体系"高级面板"化重做、多端、真实地图。

---

## 8. 已确认的关键决策

1. ✅ 模块重组 M1–M5 + 评估体系降级隐藏。
2. ✅ 模型范围:**真模型做提炼 + 规律发现,配统计兜底 + 最小样本门槛**(替换原"方案乙"的预置归因)。
3. ✅ 冷启动/批量导入 **进 5 天范围**。
4. ✅ 5 天 = **本地可跑真实切片**;云写入后置。
5. ✅ 删除 `manifest.json`/`pages.json` 死文件。
6. ✅ 内置 DeepSeek;演示联网;Key 不上云。

---

## 9. 下一步

进入 `writing-plans`,产出分阶段、带验证检查点的实施计划。

---

## 10. 需求修正(2026-06-22):经验三态 / 分类状态机拆轴

**问题**:原 `enforceAnalysisContract` 用「方向」当"能否成规则"的闸门(`非正向 → watch`),把所有负向经验一律降为"待观察",丢失了负向经验(教训/避坑)的价值——而这恰是"减少重复犯错"的核心。表现:演示导出里 23 条全是"待观察经验"。

**根因**:状态模型把正交的三轴压扁(`reusability` 里混入 `watch` 成熟度;`analysisType` 的 counterexample/constraint 活不过契约),且迁移用 direction 当闸门。

**修正(拆轴)**:
- 新增 `kind` 轴:`strategy`(正向策略)/ `caution`(负向避坑)/ `watch`(信息不足),与 `reusability`(质量)正交。
- 迁移改用「类型 + 结构」判据:负向伪装正向→拒绝;方向不明/显式watch/低置信→watch;适用条件<2→watch;正向+rule→strategy;负向/混合+counterexample/constraint→caution(reusability medium/high,保留模型标题);其它不一致→watch 兜底。
- 影响:负向经验可沉淀为"避坑规则"(有意义标题),不再清一色"待观察";旧"非正向→watch"测试已更新,新增 caution/watch 两用例。
- 待办(可选 UI):规则卡按 `kind` 区分配色/图标(策略 vs 避坑)。
